# Gemini를 통해 배운 오늘의 교훈

오늘 Jupyter Notebook으로 작성된 인터랙티브 대시보드 프로젝트를 여러 개의 독립적인 Python 파일로 분리하는 작업을 진행했습니다. 이 과정을 통해 다음과 같은 중요한 소프트웨어 엔지니어링 원칙과 교훈을 얻었습니다.

### 1. 용도에 맞는 도구 선택: Jupyter Notebook vs. Python 스크립트

- **Jupyter Notebook**: 데이터 탐색, 빠른 프로토타이핑, 튜토리얼 작성 등 인터랙티브한 개발 환경에 매우 유용합니다. 코드와 문서, 실행 결과를 한곳에서 볼 수 있어 아이디어를 시각화하고 공유하기 좋습니다.
- **Python 스크립트 (`.py`):** 애플리케이션의 배포, 재사용, 모듈화가 필요할 때는 독립적인 스크립트 파일이 더 적합합니다. 버전 관리, 테스트, 자동화된 실행 등 보다 체계적인 소프트웨어 개발 프로세스에 필수적입니다.

> **교훈:** 초기 아이디어 탐색은 Jupyter Notebook으로 시작하더라도, 기능이 구체화되고 재사용성이 중요해지면 별도의 Python 스크립트로 리팩터링하는 것이 바람직합니다.

### 2. 모듈성과 관심사 분리 (Separation of Concerns)

- **기존 문제:** 모든 대시보드 애플리케이션과 데이터 생성 로직이 하나의 Notebook에 섞여 있어 코드의 재사용이 어렵고 유지보수가 복잡했습니다.
- **개선 방안:**
    - 각기 다른 기능을 가진 대시보드 애플리케이션을 `01_basic_dashboard.py`, `02_advanced_dashboard.py` 등 개별 파일로 분리했습니다.
    - 여러 애플리케이션에서 공통으로 사용하는 데이터 생성 로직을 `data.py`라는 별도의 모듈로 추출했습니다.
- **결과:** 코드의 중복이 사라지고, 각 파일은 하나의 명확한 책임만 갖게 되어 구조가 훨씬 깔끔해졌습니다. 예를 들어, 향후 데이터 소스가 변경되더라도 `data.py` 파일만 수정하면 됩니다.

### 3. 체계적인 프로젝트 구조화

- 단순히 파일만 분리하는 것을 넘어, `dashboard`라는 하위 디렉터리를 만들어 관련된 애플리케이션 스크립트들을 그룹화했습니다.
- 캐시 파일이 저장되는 `cache-directory`와 같이 버전 관리가 필요 없는 파일들을 무시하기 위해 `.gitignore` 파일을 추가했습니다.

> **교훈:** 잘 정리된 디렉터리 구조는 프로젝트의 이해도를 높이고, 다른 사람이 프로젝트에 참여하거나 미래의 내가 코드를 다시 볼 때 큰 도움이 됩니다.

### 4. 의존성 관리의 중요성

- 프로젝트를 실행하는 데 필요한 라이브러리(dash, plotly, pandas 등)와 그 버전을 `requirements.txt` 파일에 명시했습니다.
- **결과:** 이 파일을 통해 누구나 동일한 개발 환경을 쉽게 구축하고 애플리케이션을 안정적으로 실행할 수 있습니다. 이는 프로젝트의 재현성(Reproducibility)을 보장하는 핵심적인 요소입니다.

### 5. 모듈화된 프로젝트에서의 Python 경로 관리

- `dashboard` 디렉터리 내의 스크립트가 상위 디렉터리에 있는 `data.py` 모듈을 가져올 수 있도록 `sys.path.append()`를 사용했습니다.
- 이는 Python 프로젝트를 여러 디렉터리와 모듈로 구조화할 때 흔히 사용되는 기법으로, Python의 모듈 검색 경로를 동적으로 확장하는 방법을 이해하는 계기가 되었습니다.

## 요약

오늘은 단순히 코드를 파일로 나누는 작업을 넘어, **탐색적 프로토타입(Jupyter Notebook)을 유지보수와 재사용이 용이한 애플리케이션(모듈화된 `.py` 스크립트)으로 전환**하는 실질적인 소프트웨어 엔지니어링 과정을 경험했습니다.

이를 통해 얻은 **모듈성, 관심사 분리, 의존성 관리, 체계적인 프로젝트 구조화** 등의 원칙은 앞으로 더 복잡하고 규모 있는 프로젝트를 진행하는 데 훌륭한 밑거름이 될 것입니다.
