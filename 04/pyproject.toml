[project]
name = "pandas-advanced"
version = "0.1.0"
description = "Pandas 고급 데이터 분석 예제"
dependencies = [
    "pandas>=2.0.0",
    "numpy>=1.24.0",
    "matplotlib>=3.7.0",
    "seaborn>=0.12.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
